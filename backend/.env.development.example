# Development environment settings
# Copy this file to .env.development and update with your actual values

# MongoDB Atlas connection
DATABASE_URL=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?appName=Cluster0
MONGODB_DB_NAME=cannerai_db

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_APP=app.py

# Security
SECRET_KEY=dev-secret-key-change-in-production

# Auth Configuration
# AUTH_BACKEND_URL: Used for backend-to-backend API calls (code validation)
#   - Use 'localhost' when running Flask locally
#   - Use 'host.docker.internal' when running Flask in Docker
# AUTH_FRONTEND_URL: Used for browser redirects (user login page)
#   - Always use 'localhost' (what users see in browser)
AUTH_BACKEND_URL=http://localhost:3000
AUTH_FRONTEND_URL=http://localhost:3000

# Gemini API Key
GEMINI_API_KEY=your-gemini-api-key

# JWT Secret (for extension authentication)
# MUST match FastAPI backend secret for JWT verification
JWT_SECRET_KEY=your-jwt-secret-key
