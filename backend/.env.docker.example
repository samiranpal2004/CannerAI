# Docker environment settings
# Copy this file to .env.docker and update with your actual values

# MongoDB Atlas connection
DATABASE_URL=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?appName=Cluster0
MONGODB_DB_NAME=cannerai_db

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_APP=app.py

# Security
SECRET_KEY=your-secret-key-change-in-production

# Auth Configuration for Docker
# When Flask runs in Docker, use host.docker.internal to access host machine services
AUTH_BACKEND_URL=http://host.docker.internal:3000
AUTH_FRONTEND_URL=http://localhost:3000

# Gemini API Key
GEMINI_API_KEY=your-gemini-api-key

# JWT Secret (for extension authentication)
JWT_SECRET_KEY=your-jwt-secret-key
