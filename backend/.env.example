# ⚠️  IMPORTANT: This is a template file!
# 
# To use this file:
# 1. Copy this file: cp .env.example .env.development
# 2. Replace <username>, <password>, <cluster> with your actual values
# 3. Never commit .env.development to git (it's in .gitignore)
#
# This .env.example file is safe to commit and share publicly

# MongoDB Database Configuration
# Option 1: MongoDB Atlas (Cloud - Recommended for production)
DATABASE_URL=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=cannerai_db

# Option 2: Local MongoDB (For development)
# DATABASE_URL=mongodb://localhost:27017/
# MONGODB_DB_NAME=cannerai_dev

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_APP=app.py

# Security (Change in production!)
SECRET_KEY=dev-secret-key-change-in-production

GEMINI_API_KEY=your-gemini-api-key-here

# Application Settings
PORT=5000

# Instructions:
# 1. Copy this file to .env.development (for local development)
# 2. Replace <username>, <password>, and <cluster> with your MongoDB Atlas credentials
# 3. Or use local MongoDB by uncommenting Option 2 and commenting Option 1
# 4. Never commit .env files with actual credentials to version control
